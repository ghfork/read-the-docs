Tricks
======

Clear list:

.. code-block:: python

    a[0:len(a)]
    a[:] = []
    del a[:]

pip install -r requirements.txt

List all modules

.. code-block:: python
    
    >>> help(modules)

List module functions

.. code-block:: python
    
    >>> dir(modulename)
