Hexo
====

.. code-block:: bash
    
    $ npm install hexo -g
    $ hexo init blog
    $ cd blog
    $ npm install
    $ hexo server

.. code-block:: bash

    $ hexo new [layout] <title>
    $ hexo generate
    $ hexo deploy
